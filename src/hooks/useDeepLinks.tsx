/**
 * Deep Links hook - STUBBED (Capacitor removed)
 * 
 * Returns no-op functions.
 * Capacitor will be added back incrementally to identify the black screen cause.
 */

export function useDeepLinks() {
  // No-op - Capacitor removed
}

export async function checkInitialDeepLink(): Promise<string | null> {
  return null;
}
